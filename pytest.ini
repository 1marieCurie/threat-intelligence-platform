[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    integration: tests requiring integration infrastructure such as PostgreSQL
    external: tests requiring access to a live third-party API
    external_unstable: tests relying on large or unstable live external endpoints